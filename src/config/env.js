// Backend URL — обновить на реальный Render URL после деплоя
// Локально используется http://localhost:3000 (см. auth.js getApiBaseUrl())
// Для разработки на LAN — измени на IP адрес машины с backend
export const BACKEND_URL = 'https://ekitili-backend-test.onrender.com';
